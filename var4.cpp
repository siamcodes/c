#include <stdio.h>
int main()
{
    int apple = 3;
    int orange = 2;
    int total = apple + orange;

    printf("%d apples and %d oranges\nI have %d fruits in total", apple, orange, total);
   // printf("I have %d fruits in total", total);

    return 0;
}
